version: '3'
services:
  publisher:
    build:
      context: ./
    image: 'ancolin/publisher:latest'
    ports:
      - '10000:80'
    depends_on:
      - redis
  redis:
    image: 'redis:latest'
    volumes:
      - './redis_data:/data'
